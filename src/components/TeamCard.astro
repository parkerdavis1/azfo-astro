---
const {
    slug,
    data: { name, title, blurb },
} = Astro.props.data;

const birdEmojis = ['🕊️', '🦅', '🦆', '🦜', '🐥'];
---

<p>
    {birdEmojis[Math.floor(Math.random() * birdEmojis.length)]}
    <a href=`/about/${slug}`>
        {name} - {title}
    </a>
</p>
